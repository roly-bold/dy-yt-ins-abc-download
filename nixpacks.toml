# Railway Nixpacks configuration for ClipGrab
# Next.js app with yt-dlp binary dependency installed via pip

[phases.setup]
aptPackages = ["python3", "python3-pip"]

[phases.install]
cmds = ["pip3 install --break-system-packages yt-dlp"]

[start]
cmd = "npx next start -p ${PORT:-3000}"
